/// Add one to the input value and return the new value
///
/// # Examples
///
/// ```
/// let x = 41;
/// let y = cargo_crate_mod::arithmetic::add_one(x);
///
/// assert_eq!(y, 42);
/// ```
pub fn add_one(x: i32) -> i32 {
    x + 1
}
